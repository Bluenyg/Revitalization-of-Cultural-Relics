# RAG 核心框架
langchain==0.1.0
langchain-community==0.0.10
langchain-core==0.3.81

# 向量数据库
chromadb==0.4.22

# Web 界面框架
streamlit==1.30.0

# 辅助工具
pypdf==3.17.4       # 如果后续需要加载 PDF 资料
watchdog==3.0.0     # Streamlit 文件监控
tiktoken            # 计算 token 数量（可选，但推荐）

markdown
unstructured